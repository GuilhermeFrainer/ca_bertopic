extends: "datasets/gadarian.yaml"

experiment:
  name: "gadarian_stm_test"
  random_state: 36201624
  prevalence_formula: "~ as.factor(treatment) + as.factor(partisanship) + as.factor(treatment):as.factor(partisanship)"

models:
  - id: "stm_k5"
    parameters:
      k: 5
  - id: "stm_k10"
    parameters:
      k: 10
