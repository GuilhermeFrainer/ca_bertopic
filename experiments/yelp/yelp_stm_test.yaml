extends: "datasets/yelp.yaml"

experiment:
  name: "yelp_stm_test"
  random_state: 36201624
  dataset_path: "data/processed/yelp_s10000_embeddings.parquet"
  prevalence_formula: "~ date + stars + user_average_stars + yelping_since + business_stars + as.factor(state)"

models:
  - id: "stm_k5"
    parameters:
      k: 5
  - id: "stm_k10"
    parameters:
      k: 10
